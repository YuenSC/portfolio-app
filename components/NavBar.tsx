import { memo } from "react";

const NavBar = () => {
  return (
    <div className="flex items-center justify-between">
      <div>Calvin Yuen</div>
      <div></div>
    </div>
  );
};

export default memo(NavBar);
